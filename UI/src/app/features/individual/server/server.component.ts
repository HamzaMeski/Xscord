import {Component} from "@angular/core";
import {RouterLink} from "@angular/router";

@Component({
	standalone: true,
	selector: 'server',
	imports: [

	],
	template: `
    <section>
      server
    </section>
  `
})
export class ServerComponent  {
}
