spring:
  application:
    name: SERVER

  datasource:
    url: jdbc:postgresql://localhost:5432/xscord
    username: hamza
    password: hamza

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: Africa/Casablanca


